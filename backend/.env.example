# Este arquivo serve como TEMPLATE para configuração do projeto.
# NÃO contém valores reais/secretos 
# 
# COMO USAR:
# 1. Copie este arquivo e renomeie para: .env
# 2. Substitua os valores de exemplo pelos valores reais
# 3. O arquivo .env (com valores reais) NÃO deve ser commitado

# Database
# String de conexão com o banco PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/nome_banco?schema=public
DATABASE_URL="postgresql://usuario:senha@localhost:5432/viagemtrack?schema=public"

# Server
# Porta onde o servidor backend irá rodar
PORT=3000
# Ambiente de execução (development, production, test)
NODE_ENV=development

# JWT Secret (JSON Web Token)
# Chave secreta para gerar tokens de autenticação
# Quando um usuário faz login, o backend gera um token JWT assinado com esta chave
# Este token é enviado ao frontend e usado para autenticar requisições 
# IMPORTANTE: Use uma string longa e aleatória em produção minimo 32 caracteres
# Exemplo de geração segura: openssl rand -base64 32
JWT_SECRET=sua_chave_aqui_12345

# GeoNames API
# Username para acessar a API do GeoNames (busca de cidades/países)
# https://www.geonames.org/login
GEONAMES_USERNAME=seu_username_geonames

# WeatherAPI
# Chave de acesso para a API de clima
# https://www.weatherapi.com/signup.aspx
WEATHER_API_KEY=sua_chave_weatherapi

# CORS
# URL do frontend para configuração de CORS (Cross-Origin Resource Sharing)
FRONTEND_URL=http://localhost:5173
